Take in:
  IP Range
  Output file
  Output format
  Level:
    1: only shodan; separate output files (found and not found)
    2: shodan + nmap
    3: shodan + nmap service scan and vulnerability suggestions
  Speed Options:
    default (rustscan default)
    --silent (nmap slow and silent scan)
    --aggressive (rustscan/massscan increased batch size)


Data Flow:
  Main function -> parse arguments:
    IP Range -> scan -> shodan
    Output File -> Global variable -> shodan
    Output Format -> Global variable -> shodan
    Speed Options -> scan

File Logic:
  Check if path exists
    create ips.txt, shodan-hostnames.txt file
  In the scan function first write the range with ip addresses
